<h1>Edit Articles</h1>
<%= form_for(@article, :url => "/articles/#{@article.id}", :method=>:put) do |f| %>
<% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  	
	<div><%= f.label :title %><br />
  <%= f.text_field :title %>
	</div>
	
<div><%= f.label :content %><br />
  <%= f.text_area :content, :id => "content", :rows=>"5", :cols=>"10" %>
	</div>

  <div>
	<%= f.submit "Save" %>
	<%= button_to "Cancel", { :action => 'index'}%>
</div>
	</div>
<% end %>

